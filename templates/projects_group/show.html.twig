{% extends 'base.html.twig' %}

{% block title %}ProjectsGroup{% endblock %}

{% block body %}
    <h1>ProjectsGroup</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ projects_group.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ projects_group.name }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ projects_group.createdAt ? projects_group.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ projects_group.updatedAt ? projects_group.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_projects_group_index') }}">back to list</a>

    <a href="{{ path('app_projects_group_edit', {'id': projects_group.id}) }}">edit</a>

    {{ include('projects_group/_delete_form.html.twig') }}
{% endblock %}
