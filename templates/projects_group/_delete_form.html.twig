<form method="post" action="{{ path('delete_projects_group_web', {'id': projects_group.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ projects_group.id) }}">
    <button class="btn">Delete</button>
</form>
